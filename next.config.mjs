/** @type {import('next').NextConfig} */
const nextConfig = {
  // Firebase Storage download URLs are served from these hosts.
  // (Only needed if you later use next/image with remote audio thumbnails.)
};

export default nextConfig;
